---
import '../styles/global.css';
interface Props {
  title: string;
  description?: string;
}
const {
  title,
  description = 'XP-Flow — El único framework de gestión diseñado desde adentro con Extreme Programming.',
} = Astro.props;
---

<!doctype html>
<html lang="es-419">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={`${title} — XP-Flow`} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={`${title} — XP-Flow`} />
    <meta name="twitter:description" content={description} />
    <title>{title} — XP-Flow</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <style>
      html {
        background-color: #09090b;
        scroll-behavior: smooth;
      }
    </style>
  </head>
  <body class="noise-bg min-h-screen antialiased bg-zinc-950 text-zinc-100">
    <slot />
  </body>
</html>
